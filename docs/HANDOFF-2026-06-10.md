# Handoff — 2026-06-10 (sessão tarde)

HEAD: `187bb5b` · branch `main` · working tree limpo · tudo deployado e no ar.

## O que foi feito hoje (commits desta sessão, do mais novo ao mais antigo)

| Commit | Resumo |
|---|---|
| `187bb5b` | chore: redispara deploy (repo voltou a público) |
| `2d8de4b` | chore: redispara build (falha era bloqueio Vercel, não código) |
| `fada990` | **Comparativo: remove botão "Pontuar tudo automaticamente"** (confundia o Francis; auto já vem preenchido) |
| `53f2576` | Manual: modo leitura = fit-ALTURA (página inteira na altura, sem scroll) |
| `0aff87c` | Manual: zoom do modo leitura relativo à tela + corrige recálculo (isFullscreen nas deps) |
| `3d135e2` | Manual: navegação por scroll do mouse (anti-esbarrão) + índice fixo permanente na tela cheia |
| `9877dd1` | Landing: remove AuthProvider do bundle público |
| `ab7aafb` | Landing: `/admin` deixa de revelar a URL do painel (cai na home) |
| `73f6f72` | Landing V4: conserta sticky do livro — `overflow-x-clip` no root (era `-hidden`) |
| `d220310` | Landing V4: UX writing do hero (CTAs) |
| `af232b4` | Landing: capa do Código sem fundo branco + CONTENT_VERSION bump |
| `60b4634` | Landing V4: luz única entre seções, livro sem fundo, hero 2 CTAs |
| `1864bbc` | Landing V4: **perf -94% no bundle** (base64 extraído), grade contínua, contato 3 colunas |
| `1dd1ae2` | Landing: alinha fallbacks PricingSection |
| `55cbf82` | chore: redispara deploy landing (webhook pulou) |
| `79e3bec` | Landing V4: ContentData sincronizado + plataforma em 3 janelas |
| `4f9eb4c` / `0cc7a75` / `c31ffa6` / `d0eb338` | Landing V4 P3/P2/P1/P0 (poster, pagamento premium, copy anti-IA, CTA mobile/CNPJ/FAQ) |

Além de mudanças aplicadas **direto no banco Supabase** (não em commit) — ver "Segurança" abaixo.

## Estado por frente

**Landing V4** (`apps/landing`, rota `/v4`): auditoria visual/UX/anti-IA executada e aplicada — copy na voz do comprador, perf, SEO/OG, CTA mobile, hero com 2 CTAs, sticky, grade contínua. No ar.

**Plataforma — Manual** (`apps/platform/.../manual/manual-reader.tsx`): scroll do mouse navega (anti-esbarrão: limite topo/fundo + overscroll 90px + trava 650ms), índice fixo permanente na tela cheia, zoom = fit-altura (~111% no monitor). No ar.

**Plataforma — Comparativo/Scoring**: botão "Pontuar tudo automaticamente" removido (auto é o padrão e já preenche tudo). Fórmula `(nota/10)×peso` confere com a spec do Francis. Ver memória `scoring-indice-confiabilidade`.

**Segurança Supabase** (aplicado via Management API + PAT do `.env`): `anon` revogado de tudo, só SELECT em `landing_sections`/`landing_globals`/`legal_docs`. `authenticated` = só CRUD (sem TRUNCATE/REFERENCES/TRIGGER). RLS ON em 22/22. `/admin` não revela painel. Sem secret no bundle. Ver memória `seguranca-supabase-rls`.

## PENDÊNCIAS (precisam de decisão externa — não é código travado)

1. **⭐ Combinação Empresa + Tecnologia no Índice de Confiabilidade.** Hoje o motor funde 50/50 (`scoring.ts` → `totalScore(companyScore, technicalScore)`). O Francis nunca definiu o peso entre grupos. Aguardando ele decidir: índice único 50/50 / dois índices separados / outra divisão. Mensagem pronta foi enviada a ele pelo Gabriel.
2. Exemplo "RENOVA = 64,2" do Francis usa pesos ANTIGOS; com os pesos atuais dá ~67,9 (não é bug). Confirmar com ele.
3. Critério sem dado vira "—" (renormaliza, não penaliza); "Não recomendado" = 0 (penaliza). Confirmar entendimento.

## ARMADILHAS (ler antes de mexer)

- **Deploy Vercel:** projetos na conta `francis-solarbuyside` (fora do MCP do Gabriel). Deploy por push no GitHub. **Repo DEVE ficar PÚBLICO** — privar bloqueia o deploy (Vercel Hobby não deploya repo privado de org: "Deployment was blocked"). Privar só com Vercel Pro. Webhook às vezes pula commit → re-trigger com commit vazio. Verificar deploy: `gh api repos/solarbuyside/solarbuyside-platform/deployments` + `/statuses`.
- **CMS da landing:** conteúdo vem do Supabase (`landing_sections`) com `override=true` — **o banco vence o ContentData.ts**. Mudar copy = editar o banco (via service_role) OU o admin; mexer só no ContentData.ts não aparece no site. Bump `CONTENT_VERSION` invalida cache localStorage. Detalhe em memória `landing-deploy-e-cms`.
- **Fonte de verdade do scoring:** `apps/platform/src/domain/comparisons/` (CLAUDE.md). Não duplicar lógica fora dali.
- **Teste de secret:** usar `grep -roF`/contagem, nunca `grep -l ... | head &&` nem `git grep -l "$jwt"` (mascaram exit / JWT vira regex → falso positivo).

## Memórias do projeto (carregam automático)
`landing-deploy-e-cms`, `seguranca-supabase-rls`, `scoring-indice-confiabilidade`, `brevo-greenn-epic`, `supabase-migrations-manual-apply`.
